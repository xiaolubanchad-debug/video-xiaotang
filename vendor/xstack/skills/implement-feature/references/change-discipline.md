# change discipline

Use these rules to keep implementation tight.

- Prefer the smallest change that fully solves the task.
- Do not mix cleanup with behavior changes unless required.
- Match existing repository conventions before introducing new patterns.
- Leave clear notes when a compromise is temporary.
- If a larger fix is needed than originally expected, stop and re-plan instead of silently expanding scope.
