# ai-slop signals

Watch for these patterns:

- Generic labels like “Manage”, “Optimize”, or “Proceed” without context
- Too many panels, cards, or gradients without hierarchy
- Inconsistent button styles or spacing across adjacent elements
- Empty states that say nothing useful
- Error states that blame the user or provide no next step
- Interface density that feels generated rather than designed
